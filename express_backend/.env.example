# Server
PORT=3001
HOST=0.0.0.0

# Database
MONGODB_URI=mongodb://localhost:27017/analytics

# CORS
# Allowed frontend origin(s); primary origin used by Socket.io and CORS.
FRONTEND_ORIGIN=http://localhost:3000
# When STRICT_CORS=true only allow explicit local origins in server code; otherwise wide-open for dev.
STRICT_CORS=false

# Auth
# REQUIRED in production – used to sign/verify JWTs
JWT_SECRET=replace-with-a-strong-secret
# Token expiration: e.g., 1d, 12h, 30m
TOKEN_EXPIRES_IN=1d

# Node environment (development|production)
NODE_ENV=development
