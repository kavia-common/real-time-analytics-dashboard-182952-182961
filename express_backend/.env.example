# Server
PORT=3001
HOST=0.0.0.0

# Mongo
MONGODB_URI=mongodb://localhost:27017/analytics_demo

# CORS
FRONTEND_ORIGIN=http://localhost:3000
STRICT_CORS=false

# Auth
JWT_SECRET=change-me
TOKEN_EXPIRES_IN=1d

# Admin bootstrap (preferred)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=adminpassword
ADMIN_USERNAME=admin

# Legacy admin bootstrap (supported for backward compatibility)
# If ADMIN_* are set, these are ignored. Otherwise these will be used.
VITE_ADMIN_EMAIL=
VITE_ADMIN_PASSWORD=
VITE_ADMIN_USERNAME=

# Maintenance token for one-time seed endpoint
# Note: Used by POST /api/admin/seed via X-Maintenance-Token header
VITE_MAINTENANCE_TOKEN=
